# placeholder: regenerated by CI
# Illustrative skeleton only. The `example` token is never a real backend, so the bootstrap never
# installs this file. A real requirements/utilities.<backend_token>.txt is a fully-resolved, hashed
# `uv pip compile` output of horde-image-utilities[server,annotators_<X>,rembg_<X>,mediapipe]==<version>.
