# Files bundled into the release zip, read by .github/workflows/release.yml.
# One path or glob per line; blank lines and lines starting with '#' are ignored.
# The horde_worker_regen/ and worker_bootstrap/ packages are copied (recursively) separately by the workflow.
# A test (tests/test_bundle_manifest.py) asserts every root .cmd/.sh launcher is listed here, so a new
# launcher fails CI until added, and that no listed path is stale (e.g. a removed script).
pyproject.toml
uv.lock
LICENSE
# Install-time disclosure shown by every front-end: the plain-language notice (also the .exe's Info page)
# and the aggregated third-party license notices (also the .exe's License page).
INSTALL_NOTICE.txt
THIRD-PARTY-NOTICES.md
README.md
README_advanced.md
bridgeData_template.yaml
# The stdlib-only bootstrap entry point the launchers run via `uv run --script` (its logic lives in the
# worker_bootstrap/ package, copied separately by the workflow alongside horde_worker_regen/).
bootstrap.py
download_models.py
run_worker.py
convert_config_to_env.py
update-runtime.cmd
update-runtime.sh
update-runtime-rocm.sh
runtime.cmd
runtime.sh
runtime-rocm.sh
horde-bridge.cmd
horde-bridge.sh
horde-bridge-rocm.sh
horde-worker.cmd
horde-worker.sh
preload-models.cmd
preload-models.sh
# GPU detector for the graphical (Inno) installer's pre-install wizard, which must warn AMD/CPU users
# before uv exists (so it cannot use bootstrap.py's detector yet). Runtime/one-liner detection now lives
# in worker_bootstrap/detect.py; this stays as the wizard's pre-uv equivalent. Staging flattens it to the
# bundle root so the installer sources it from the same staging as the zip.
packaging/detect-backend.ps1
