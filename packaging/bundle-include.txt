# Files bundled into the release zip, read by .github/workflows/release.yml.
# One path or glob per line; blank lines and lines starting with '#' are ignored.
# The horde_worker_regen/ package is copied separately by the workflow.
# A test (tests/test_bundle_manifest.py) asserts every root .cmd/.sh launcher is listed here, so a new
# launcher fails CI until added, and that no listed path is stale (e.g. a removed script).
pyproject.toml
uv.lock
LICENSE
README.md
README_advanced.md
bridgeData_template.yaml
download_models.py
run_worker.py
convert_config_to_env.py
update-runtime.cmd
update-runtime.sh
update-runtime-rocm.sh
runtime.cmd
runtime.sh
runtime-rocm.sh
horde-bridge.cmd
horde-bridge.sh
horde-bridge-rocm.sh
horde-worker.cmd
horde-worker.sh
preload-models.cmd
preload-models.sh
# Shared GPU detector used by install.ps1 (after extraction) and the graphical installer; staging
# flattens it to the bundle root, so install.ps1 reads it from the install dir, not packaging/.
packaging/detect-backend.ps1
